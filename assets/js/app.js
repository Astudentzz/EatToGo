
const ETG={restaurants:[{id:1,name:'Sakura Omakase',cuisine:'Japanese',rating:4.8,loc:'Kuala Lumpur',price:'RM25-60',emoji:'🍣',deal:'15% student deal'},{id:2,name:'Spice Garden',cuisine:'Indian',rating:4.7,loc:'Skudai',price:'RM12-35',emoji:'🍛',deal:'Free drink'},{id:3,name:'Pasta Street',cuisine:'Italian',rating:4.6,loc:'Johor Bahru',price:'RM18-45',emoji:'🍝',deal:'Combo save RM8'},{id:4,name:'Burger Lab',cuisine:'Western',rating:4.5,loc:'Senai',price:'RM15-40',emoji:'🍔',deal:'Lunch promo'}],menu:[{id:1,cat:'Popular',name:'Salmon Sushi Set',price:28,emoji:'🍣'},{id:2,cat:'Popular',name:'Chicken Katsu Don',price:18,emoji:'🍱'},{id:3,cat:'Hot Dishes',name:'Spicy Ramen',price:22,emoji:'🍜'},{id:4,cat:'Drinks',name:'Matcha Latte',price:9,emoji:'🍵'},{id:5,cat:'Sides',name:'Edamame',price:7,emoji:'🫛'},{id:6,cat:'Popular',name:'Tempura Set',price:26,emoji:'🍤'}]};
function $(q){return document.querySelector(q)};function $all(q){return [...document.querySelectorAll(q)]};
function get(k,d){try{return JSON.parse(localStorage.getItem(k))??d}catch(e){return d}}function set(k,v){localStorage.setItem(k,JSON.stringify(v))}
function seed(){if(!get('users',null))set('users',[{name:'Navin Ramu',email:'customer@eattogo.test',role:'customer'},{name:'Restaurant Staff',email:'staff@eattogo.test',role:'staff'},{name:'Restaurant Owner',email:'owner@eattogo.test',role:'owner'},{name:'Admin',email:'admin@eattogo.test',role:'admin'}]);if(!get('bookings',null))set('bookings',[{id:'B1001',customer:'Navin Ramu',restaurant:'Sakura Omakase',date:'2026-05-10',time:'7:30 PM',guests:2,status:'pending',arrival:'Not arrived',payment:'Pay at counter'}]);if(!get('requests',null))set('requests',[{id:'R001',owner:'Ng Yue Yang',restaurant:'Yang Noodles',status:'pending'}]);if(!get('staffOrders',null))set('staffOrders',[{id:'O3001',booking:'B1001',items:'Sushi Set x2, Matcha x1',status:'Preparing'}])}
seed();
function nav(){let u=get('session',null);let box=$('#authArea'); if(box) box.innerHTML=u?`<span class="me-2 fw-bold">${u.name}</span><button onclick="logout()" class="btn btn-outline-etg btn-sm">Sign Out</button>`:`<a href="login.html" class="btn btn-outline-etg btn-sm">Sign In</a>`}
function login(e){e.preventDefault();let role=$('#role').value,email=$('#email').value;let users=get('users',[]);let u=users.find(x=>x.role==role)||{name:role,email,role};set('session',u);location.href= role=='admin'?'admin.html':role=='staff'?'staff-dashboard.html':role=='owner'?'owner-dashboard.html':'index.html'}
function signup(e){e.preventDefault();let u={name:$('#name').value,email:$('#email').value,role:$('#role').value};let users=get('users',[]);users.push(u);set('users',users);set('session',u);location.href=u.role=='owner'?'owner-dashboard.html':'index.html'}function logout(){localStorage.removeItem('session');location.href='login.html'}
function forgot(e){e.preventDefault();alert('Demo: password reset link has been sent. Backend teammate can connect email later.');location.href='login.html'}
function renderRestaurants(list=get("customRestaurants",ETG.restaurants)){let el=$('#restaurantList');if(!el)return;el.innerHTML=list.map(r=>`<div class="col-md-6 col-lg-3"><div class="restaurant-card h-100"><div class="food-img">${r.emoji}</div><div class="p-3"><span class="badge-etg">${r.deal}</span><h4 class="mt-3">${r.name}</h4><p class="text-muted mb-1">${r.cuisine} • ${r.loc}</p><p>⭐ ${r.rating} • ${r.price}</p><a class="btn btn-etg w-100" href="restaurant.html?id=${r.id}">View & Reserve</a></div></div></div>`).join('')}
function searchHome(e){e.preventDefault();let q=$('#q')?.value||'';location.href='search-results.html?q='+encodeURIComponent(q)}
function filterResults(){let q=($('#q')?.value||new URLSearchParams(location.search).get('q')||'').toLowerCase();let cuisine=$('#cuisine')?.value||'';let list=ETG.restaurants.filter(r=>(!q||r.name.toLowerCase().includes(q)||r.cuisine.toLowerCase().includes(q)||r.loc.toLowerCase().includes(q))&&(!cuisine||r.cuisine==cuisine));renderRestaurants(list)}
function selectSlot(el){$all('.slot').forEach(s=>s.classList.remove('active'));el.classList.add('active');$('#selectedTime')&&($('#selectedTime').textContent=el.textContent)}
function reserve(){let b={id:'B'+Date.now().toString().slice(-5),customer:get('session',{name:'Guest'}).name,restaurant:'Sakura Omakase',date:$('#resDate')?.value||'2026-05-10',time:$('#selectedTime')?.textContent||'7:30 PM',guests:$('#guests')?.value||2,status:'pending',arrival:'Not arrived',payment:'Pay at counter'};let arr=get('bookings',[]);arr.unshift(b);set('currentBooking',b);set('bookings',arr);location.href='preorder.html'}
function renderMenu(){let el=$("#menuList");if(!el)return;let menuData=get("customMenu",ETG.menu);el.innerHTML=menuData.map(i=>`<div class="col-md-6"><div class="card-etg p-3 d-flex flex-row align-items-center gap-3 mb-3"><div style="font-size:44px">${i.emoji}</div><div class="flex-grow-1"><h5>${i.name}</h5><small class="text-muted">${i.cat}</small><div class="fw-bold text-warning">RM ${i.price.toFixed(2)}</div></div><button class="btn btn-etg" onclick="addCart(${i.id})">+</button></div></div>`).join('');renderCart()}
function addCart(id){let cart=get('cart',{});cart[id]=(cart[id]||0)+1;set('cart',cart);renderCart()}function removeCart(id){let cart=get('cart',{});delete cart[id];set('cart',cart);renderCart()}function qty(id,n){let cart=get('cart',{});cart[id]=Math.max(0,(cart[id]||0)+n);if(!cart[id])delete cart[id];set('cart',cart);renderCart()}
function renderCart(){let el=$('#cartItems');if(!el)return;let cart=get('cart',{}),total=0;let rows=Object.entries(cart).map(([id,q])=>{let item=(get("customMenu",ETG.menu)).find(x=>x.id==id)||ETG.menu.find(x=>x.id==id);total+=item.price*q;return`<div class="d-flex align-items-center gap-2 border-bottom py-2"><span style="font-size:28px">${item.emoji}</span><div class="flex-grow-1"><b>${item.name}</b><br><small>RM${item.price} x ${q}</small></div><button class="btn btn-sm btn-outline-secondary" onclick="qty(${id},-1)">-</button><b>${q}</b><button class="btn btn-sm btn-outline-secondary" onclick="qty(${id},1)">+</button><button class="btn btn-sm btn-danger" onclick="removeCart(${id})">×</button></div>`}).join('');el.innerHTML=rows||'<p class="text-muted text-center p-3">Cart is empty</p>';$('#total')&&($('#total').textContent='RM '+total.toFixed(2));}
function checkout(){let b=get('currentBooking',get('bookings',[])[0]);let orders=get('staffOrders',[]);orders.unshift({id:'O'+Date.now().toString().slice(-5),booking:b.id,items:'Customer pre-order cart',status:'Pending'});set('staffOrders',orders);localStorage.removeItem('cart');location.href='pay-counter.html'}
function confirmPayCounter(){let b=get('currentBooking',null);if(b){b.payment='Pay at counter - informed';set('currentBooking',b)}location.href='booking-success.html'}
function confirmArrival(){let bookings=get('bookings',[]);bookings[0]&&(bookings[0].arrival='Arrived');set('bookings',bookings);alert('Arrival confirmed. Restaurant staff has been notified.');location.href='feedback.html'}
function submitFeedback(e){e.preventDefault();let f=get('feedback',[]);f.unshift({name:get('session',{name:'Guest'}).name,rating:$('#rating').value,msg:$('#message').value});set('feedback',f);alert('Feedback uploaded successfully. Thank you!');location.href='booking-history.html'}
function renderBookings(){let el=$('#bookingRows');if(!el)return;let b=get('bookings',[]);el.innerHTML=b.map(x=>`<tr><td>${x.id}</td><td>${x.customer}</td><td>${x.restaurant}</td><td>${x.date} ${x.time}</td><td>${x.guests}</td><td><span class="status ${x.status}">${x.status}</span></td><td>${x.arrival}</td><td><button class="btn btn-sm btn-success" onclick="updateBooking('${x.id}','confirmed')">Confirm</button> <button class="btn btn-sm btn-danger" onclick="updateBooking('${x.id}','rejected')">Reject</button></td></tr>`).join('')}
function updateBooking(id,status){let b=get('bookings',[]).map(x=>x.id==id?{...x,status}:x);set('bookings',b);renderBookings()}
function renderAccounts(){let el=$('#accountRows');if(!el)return;el.innerHTML=get('users',[]).map((u,i)=>`<tr><td>${u.name}</td><td>${u.email}</td><td>${u.role}</td><td><button onclick="deleteAccount(${i})" class="btn btn-sm btn-danger">Delete</button></td></tr>`).join('')}function deleteAccount(i){let u=get('users',[]);u.splice(i,1);set('users',u);renderAccounts()}
function renderRequests(){let el=$('#requestRows');if(!el)return;el.innerHTML=get('requests',[]).map(r=>`<tr><td>${r.id}</td><td>${r.owner}</td><td>${r.restaurant}</td><td><span class="status ${r.status}">${r.status}</span></td><td><button class="btn btn-sm btn-success" onclick="requestStatus('${r.id}','confirmed')">Approve</button> <button class="btn btn-sm btn-danger" onclick="requestStatus('${r.id}','rejected')">Reject</button></td></tr>`).join('')}function requestStatus(id,status){let r=get('requests',[]).map(x=>x.id==id?{...x,status}:x);set('requests',r);renderRequests()}
function ownerRequest(e){e.preventDefault();let arr=get('requests',[]);arr.unshift({id:'R'+Date.now().toString().slice(-4),owner:$('#ownerName').value,restaurant:$('#restName').value,status:'pending'});set('requests',arr);alert('Restaurant information request submitted to admin.');e.target.reset();renderOwnerRequests()}
function renderOwnerRequests(){let el=$('#ownerRequests');if(!el)return;el.innerHTML=get('requests',[]).map(r=>`<tr><td>${r.restaurant}</td><td><span class="status ${r.status}">${r.status}</span></td><td>Admin will send request result here.</td></tr>`).join('')}
function renderStaff(){let el=$('#staffOrders');if(el)el.innerHTML=get('staffOrders',[]).map(o=>`<tr><td>${o.id}</td><td>${o.booking}</td><td>${o.items}</td><td><span class="status ready">${o.status}</span></td><td><button class="btn btn-sm btn-success" onclick="o.status='Ready';alert('Order status updated and customer notified to pay at counter.')">Update</button></td></tr>`).join('');renderBookings()}
window.addEventListener('DOMContentLoaded',()=>{nav();renderRestaurants();filterResults();renderMenu();renderBookings();renderAccounts();renderRequests();renderOwnerRequests();renderStaff();});
